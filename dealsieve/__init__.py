"""DealSieve: a persistent acquisition agent.

It rejects opportunities, remembers exactly why, computes what would change
that decision, watches for it, and interrupts a human only when it happens.
"""

__version__ = "0.1.0"
